/**
 * util.cc
 */
#include "util.h"

namespace refl {
namespace util {
arma::mat
to_arma(cluster::QuiverMatrix const& q) {
	return arma::mat();
}
arma::mat
gram(arma::mat const& vectors, arma::mat const& product){
	return arma::mat();
}
}
}
